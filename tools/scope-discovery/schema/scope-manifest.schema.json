{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://audiocontrol.org/schemas/scope-manifest.schema.json",
  "title": "Scope Discovery Manifest",
  "description": "Per-feature scope-manifest.yaml. Generated as a strawman by /scope-inventory and curated by the operator. Drives subsequent multi-agent discovery, UI Playwright walks, AST/clone audits, and the dispatch wrapper's sibling-enumeration gate. Lives at docs/<version>/<status>/<feature-slug>/scope-manifest.yaml.",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "feature_slug", "scenarios", "reference_docs", "discovery_themes"],
  "properties": {
    "kind": {
      "description": "Which discovery mode applies. ui: Playwright route walk + DOM-token snapshot. code: AST + clone-detector matrix across module file globs. hybrid: union of both, deduplicated by file-path key.",
      "type": "string",
      "enum": ["ui", "code", "hybrid"]
    },
    "feature_slug": {
      "description": "The feature directory name under docs/<version>/<status>/. Used to anchor evidence paths and journal entries.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
      "minLength": 2,
      "maxLength": 80
    },
    "version": {
      "description": "Optional schema-revision tag for the manifest itself. Bumped when the manifest format changes in a backwards-incompatible way.",
      "type": "string",
      "pattern": "^\\d+\\.\\d+(\\.\\d+)?$"
    },
    "generated_by": {
      "description": "How the manifest was produced. Strawman: machine-generated by /scope-inventory awaiting operator curation. Curated: operator has reviewed and pruned. Hand-authored: operator wrote from scratch (rare; flag for review).",
      "type": "string",
      "enum": ["strawman", "curated", "hand-authored"]
    },
    "generated_at": {
      "description": "ISO-8601 timestamp of last machine generation (set by /scope-inventory). Curation does not update this field; the journal entry is the curation record.",
      "type": "string",
      "format": "date-time"
    },
    "scenarios": {
      "description": "Named scenarios under which surfaces are exercised. For ui kind these are typically dev-server query parameters (?midi=simulated&scenario=X); for code kind these are pattern-shape labels. Must contain at least one entry.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/scenario" }
    },
    "reference_docs": {
      "description": "Authoritative documents the discovery and synthesis passes treat as ground truth: PRD, workplan, mockups, design-language references, analysis reports. Discovery-agent findings that contradict reference docs are flagged in the synthesis pass, not silently overridden.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/reference_doc" }
    },
    "discovery_themes": {
      "description": "Pattern shapes the PRD-themed discovery agents are seeded with. For a redesign feature: things like 'cross-page chrome consistency', 'list-bank chevron sizing'. For a typing-cleanup feature: 'as Type casts', 'any escapes'. Free-text labels — the agents interpret them.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 2,
        "maxLength": 200
      }
    },
    "routes": {
      "description": "UI routes to walk. Required when kind is ui or hybrid; forbidden when kind is code.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/route" }
    },
    "modules": {
      "description": "Source-code module entries to audit. Required when kind is code or hybrid; forbidden when kind is ui.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/module" }
    },
    "notes": {
      "description": "Free-text operator notes attached to the manifest. Not consumed by tooling.",
      "type": "string"
    }
  },
  "allOf": [
    {
      "if": { "properties": { "kind": { "const": "ui" } }, "required": ["kind"] },
      "then": { "required": ["routes"], "not": { "required": ["modules"] } }
    },
    {
      "if": { "properties": { "kind": { "const": "code" } }, "required": ["kind"] },
      "then": { "required": ["modules"], "not": { "required": ["routes"] } }
    },
    {
      "if": { "properties": { "kind": { "const": "hybrid" } }, "required": ["kind"] },
      "then": { "required": ["routes", "modules"] }
    }
  ],
  "$defs": {
    "scenario": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": {
          "description": "Scenario identifier — referenced by route entries (and, indirectly, by discovery-agent findings).",
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
          "minLength": 2,
          "maxLength": 80
        },
        "label": {
          "description": "Human-readable scenario label, shown in evidence captures.",
          "type": "string",
          "maxLength": 200
        },
        "description": {
          "description": "What the scenario exercises — which device state, which fixtures, etc.",
          "type": "string"
        },
        "query": {
          "description": "Query-string fragment appended to ui routes (e.g., 'midi=simulated&scenario=tones-bank-0').",
          "type": "string"
        }
      }
    },
    "reference_doc": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": {
          "description": "Repo-relative path to the reference document.",
          "type": "string",
          "minLength": 1
        },
        "role": {
          "description": "Why the discovery layer should consult this document.",
          "type": "string",
          "enum": [
            "prd",
            "workplan",
            "mockup",
            "design-language",
            "analysis-report",
            "rule",
            "other"
          ]
        },
        "summary": {
          "description": "One-line summary of what the document contributes — helps discovery agents avoid re-deriving.",
          "type": "string",
          "maxLength": 500
        }
      }
    },
    "route": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "devices", "scenarios"],
      "properties": {
        "path": {
          "description": "URL path of the route, leading slash. E.g., '/patches', '/roland/s550/editor/play'.",
          "type": "string",
          "pattern": "^/.*$",
          "minLength": 1,
          "maxLength": 200
        },
        "devices": {
          "description": "Device identifiers the route should be walked under. Multi-device editors capture once per device. Use ['none'] for device-agnostic routes.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$"
          },
          "uniqueItems": true
        },
        "scenarios": {
          "description": "Scenario ids (must reference the manifest-level scenarios[].id list). Discovery walks the cross-product (route x device x scenario).",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$"
          },
          "uniqueItems": true
        },
        "primitives": {
          "description": "Cross-page primitives the route is expected to render. Used by the synthesis pass to flag drifted consumers.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "uniqueItems": true
        },
        "skip_reason": {
          "description": "If set, the route is enumerated for completeness but skipped at capture time with this reason recorded in the journal. Reasons must be specific and non-deferral (no 'TODO', 'for now', 'later' phrasings).",
          "type": "string",
          "minLength": 4,
          "maxLength": 300
        }
      }
    },
    "module": {
      "type": "object",
      "additionalProperties": false,
      "required": ["glob", "patterns"],
      "properties": {
        "glob": {
          "description": "Source file glob (relative to repo root) for the module's AST/clone audit. E.g., 'modules/roland-sxx0-editor/src/**/*.{ts,tsx}'.",
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        },
        "label": {
          "description": "Human-readable module label.",
          "type": "string",
          "maxLength": 200
        },
        "patterns": {
          "description": "Pattern/AST shapes being inventoried in this module. Each entry names what discovery agents are looking for; the synthesis pass groups findings by pattern id.",
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/pattern" }
        },
        "exclude_globs": {
          "description": "File globs explicitly excluded from this module's audit. Each exclude must carry a reason via the parallel exclude_reasons list (same index).",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "exclude_reasons": {
          "description": "Parallel array to exclude_globs. Reasons must be specific and non-deferral.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 4,
            "maxLength": 300
          }
        }
      }
    },
    "pattern": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind"],
      "properties": {
        "id": {
          "description": "Pattern identifier — discovery findings reference this id.",
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
          "minLength": 2,
          "maxLength": 80
        },
        "kind": {
          "description": "What shape of pattern this is. ast-call: function/method call shape. ast-jsx: JSX element/prop shape. type-cast: 'as Type' or '<Type>' assertions. grep: literal string or regex match. clone-group: code-clone detector group. import: import-specifier shape.",
          "type": "string",
          "enum": ["ast-call", "ast-jsx", "type-cast", "grep", "clone-group", "import", "other"]
        },
        "description": {
          "description": "What discovery agents should look for and why it matters.",
          "type": "string",
          "maxLength": 500
        },
        "query": {
          "description": "Concrete query — regex, AST selector, or jscpd group id. Format depends on the pattern's kind; the agent interpreting it knows the convention.",
          "type": "string"
        }
      }
    }
  }
}
