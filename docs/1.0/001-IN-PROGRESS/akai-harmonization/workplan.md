# Akai Harmonization — Workplan

**Branch:** `feature/akai-harmonization`
**Worktree:** `~/work/audiocontrol-work/audiocontrol-akai-harmonization`
**Status:** ~~FEATURE COMPLETE~~ — **RE-OPENED 2026-05-25 evening** after operator visual review of the live ProgramsPage revealed the rendered UI bears only a "bizarro-world relationship to our mockups." Phase 2's structural gates (43 spec passes, 0 anti-pattern findings, 0 holdouts) verified contracts but did NOT verify visual fidelity against `mockups/programs.html` et al. The premature COMPLETE annotation is the failure mode `feedback_actually_review` memory names: computed-style checks and DOM-shape assertions aren't enough; pages need actual side-by-side visual comparison with the mockup before any complete claim. **Phase 4 (Visual fidelity review against mockups) added below; FEATURE COMPLETE re-asserted only after Phase 4 closes.** See [`implementation-summary.md`](./implementation-summary.md) for structural outcomes (still accurate); the visual-fidelity gap is enumerated in Phase 4.

## GitHub Tracking

- **Parent issue:** [#457](https://github.com/audiocontrol-org/audiocontrol/issues/457) — `[akai-harmonization] feature lifecycle parent`
- **Phase 0 (rolling bug-fix pass):** [#458](https://github.com/audiocontrol-org/audiocontrol/issues/458)
- **Phase 1 (design-language audit):** [#459](https://github.com/audiocontrol-org/audiocontrol/issues/459)
- **Phase 2 (harmonization implementation):** [#460](https://github.com/audiocontrol-org/audiocontrol/issues/460) — blocked on #459 closure
- **Phase 3 (scope-discovery on the harmonized akai surface):** [#461](https://github.com/audiocontrol-org/audiocontrol/issues/461) — blocked on #460 closure

## Four-phase mandate

This workplan covers four intentionally co-located streams running concurrently on one branch:

- **Phase 0 — Rolling Bug-Fix Pass** (operator-surfaced, open-ended). Cross-cutting; runs throughout. Each fix is one commit; no sweep refactors slipped in. Discipline: "no while-I-was-in-here."
- **Phase 1 — Design-language audit** (`/frontend-design`-gated). Produces `harmonization-spec.md` — a primitive-by-primitive, page-by-page disposition spec. No code change; audit output only.
- **Phase 2 — Harmonization implementation** (post-Phase-1 approval). Migrates akai to canonical primitives where the spec says `adopt-roland-pattern`; promotes akai patterns to canonical where the spec says `adopt-akai-pattern`; extends `editor-core` with theme-token support for dialect variance. Each primitive migration is a refactor commit with `canonical_side` declared.
- **Phase 3 — Scope-discovery on the harmonized akai surface** (post-Phase-2 stabilization). Refreshes the akai clone baseline; dispositions every akai-touching entry in `clones.yaml`; lands refactor commits per the protocol's Step 0 (canonical-side + tests-proof) discipline; backfills adopter-manifests + anti-patterns; updates the editor-symmetry matrix.

The bug-catchment pass (Phase 0) runs concurrently with the design-audit / harmonization / scope-discovery streams, in separate commits. Phase 0's "no while-I-was-in-here" discipline is incompatible with Phase 2's primitive-migration shape and Phase 3's refactor-PR shape, so the streams co-locate on the branch but stay in separate commits. Implementers must check the Phase header of any task before applying its discipline.

Phase ordering between Phases 1 → 2 → 3 is load-bearing. Running scope-discovery (Phase 3) on the un-harmonized akai surface would produce a baseline that gets invalidated the moment Phase 2's harmonization commits land. The 142 existing akai entries in `clones.yaml` will need to be refreshed (via the `migration-map` system) after Phase 2 lands.

See [`prd.md` § Problem Statement](./prd.md) for the operator-level framing.

## Technical Approach

### Modules Affected

- `modules/akai-s3k-editor` (primary write target — conforms to canonical primitives in Phase 2)
- `modules/roland-sxx0-editor` (secondary write target where akai is the better canonical, or where roland is also drifting)
- `modules/editor-core` (canonical primitives + per-editor theme/dialect tokens)
- `docs/scope-discovery/` (`clones.yaml`, `adopter-manifests.yaml`, `anti-patterns.yaml`, `editor-symmetry.md`)
- `modules/launch-control-xl3` — NOT modified. An uncommitted `CLAUDE.md` edit there is unrelated and should be committed or stashed before branching.

### Strategy

- **Phase ordering is load-bearing.** Harmonization → scope-discovery. Running scope-discovery on the un-harmonized akai surface produces a baseline that gets invalidated as soon as harmonization commits land.
- **The `/frontend-design` skill drives Phase 1.** Use it to produce side-by-side screenshots of every akai page next to its roland equivalent (where one exists) and to propose harmonization moves. The audit's output is a markdown spec, not code.
- **Dialect variance lives in design tokens, not code paths.** Per the standing CLAUDE.md guideline: "Never use conditionals in UI components to switch behavior based on device configuration." Color/font/iconography variance between akai and roland surfaces is expressed through CSS variables / theme tokens, never through `if (device === 'akai')` branches. If a harmonization step would require a code-path branch, it is not actually a dialect — it is a structural divergence that needs a different `disposition`.
- **Bilateral changes use the canonical-side discipline.** Each refactor commit that promotes a primitive declares `canonical_side` in the commit message per the scope-discovery-protocol contract. The `harmonization-spec.md` entry for that primitive is the proof-of-design for the `canonical_side` choice.
- **The controller IS the gate.** After every implementer dispatch, the controller independently re-runs `make` and the relevant `make test-ui-*` gates plus `pnpm test:scope-discovery`. No CI; sub-agent reported counts are claims until verified.
- **Bugs are fixed as found — no deferral.** Per the standing agent-discipline rules ("Just for now is bullshit" + "Drive every effort to completion before starting the next"), every bug surfaced during the audit, harmonization, or scope-discovery phases is fixed in scope, in its own commit, before the surfacing phase advances. Filing-an-issue is not a `disposition` unless the operator explicitly accepts the deferral. See Phase 0 for the rolling-bug-fix discipline.
- **Dogfooding-feedback loop on scope-discovery-protocol.** If the audit or the harmonization phase surfaces gaps in the protocol (schema, scanner partitioning, registry shape), file them as issues against the scope-discovery-protocol feature and apply fixes back before continuing.

### Dependencies

- **`scope-discovery-protocol`** (merged via PR #454, 2026-05-22). All gates and tooling in place.
- **`roland-sxx0-editor` post-roland-bugfix-merge state** (PR #456, 2026-05-23). V3 chrome is the canonical baseline.
- **`editor-core`'s existing shared primitives.** SlideDrawer, PageTitleRow, AcChevron, useExportDialogLifecycle, useStepHistory, AcRadioTabs, BankHeader, SlotInfo, etc.

## Phase 0: Rolling Bug-Fix Pass (cross-cutting)

**Deliverable:** Operator-declared completion. Any bug surfaced during the audit, harmonization, or scope-discovery phases is fixed in its own commit before the surfacing phase advances. No deferral to "a separate effort"; no "we'll handle that later"; no IOU comments.

**Tasks:** Added to the triage table below as bugs are reported. No upfront task list — this phase is intentionally open-ended.

### Bug Triage Table

| ID | Reported | Surface | Description | Status | Commit |
|----|----------|---------|-------------|--------|--------|
| BUG-EC-001 | 2026-05-24 | `modules/editor-core/test/` | 7 pre-existing tests fail on a clean baseline — `PluginLibraryBrowser.test.tsx` (2), `AcEnvelope.test.tsx` (1), `MoveDialog.test.tsx` (4). Unrelated to akai-harmonization (verified via stash + re-run); inherited from prior work. Makes `pnpm --filter @audiocontrol/editor-core test` unusable as a regression gate without manually correlating against the baseline. Triage + fix or `.skip` with a linked GitHub issue per failure. Goal: editor-core test suite returns green so it can serve as a regression gate. Was originally TF-007 in [`tooling-feedback.md`](./tooling-feedback.md); re-classified 2026-05-24 (it's a bug, not scope-discovery tooling friction). | closed | `0473616a`, `7463b072`, `dbfde688` |

**BUG-EC-001 closure note (2026-05-25):** All 7 failures triaged + fixed (no `.skip` — every test now passes its current production contract). Every failure was test contract drift (production code evolved; tests weren't updated alongside). Three commits, one per test file:
- `0473616a` — **AcEnvelope.test.tsx** (1 failure). `clicking a graph point button triggers onPointSelect` used `fireEvent.click`, but the `EnvelopeDragPoint` button wires segment-select to `onPointerDown` (so the same handler that begins a drag also selects the segment on "press without movement"). Fix: switched to `fireEvent.pointerDown`, renamed test to match the contract, added a comment explaining the pointer-event discipline.
- `7463b072` — **PluginLibraryBrowser.test.tsx** (2 failures). `renders loading state` asserted "Loading library..." text that no longer exists — the loading UI was migrated to a skeleton-band placeholder with no user-visible text. `renders empty state when no library handle` asserted "Connect to a library folder to get started." with a trailing period the production copy doesn't have. Fix: loading test now asserts the `.ac-plugin-library-browser-skeleton` wrapper + skeleton-section count; empty test drops the trailing period.
- `dbfde688` — **MoveDialog.test.tsx** (4 failures). All four open-state tests used `renderToStaticMarkup` which never runs effects, but MoveDialog was migrated to wrap `SlideDrawer`, which gates DOM via `if (!mounted) return null;` and only flips `mounted` inside a `useEffect`. Every assertion against the empty SSR string failed. Fix: rewrote the four open-state tests to use `render` from `@testing-library/react` (the closed-state test still uses SSR — that path short-circuits in MoveDialog itself before reaching SlideDrawer); centralized via a `renderOpen` helper reading from `document.body.innerHTML`; corrected the title assertion to use raw `"` characters instead of the SSR-era `&quot;` form. Editor-core suite: 391 passed / 0 failed (was 384 / 7 failed pre-dispatch).

### Discipline (mirrors roland-bugfix Phase 1)

- One bug per commit; descriptive commit messages; no sweep refactors folded in (the "no while-I-was-in-here" discipline).
- Visual verification of the reproduction (screenshot or test-harness check) before the bug-row is marked `Closed`.
- Each fix runs the load-bearing test gate (`make test-ui-akai` / `make test-ui-roland` as applicable) before claiming done; controller re-runs independently.
- Bugs accumulate in this triage table as they are discovered; no upfront task list.

### Per-Fix Acceptance Criteria

Each fix must satisfy all of:

- Visual verification of the reproduction (screenshot or test-harness check)
- Load-bearing test gate green at the fix commit (`make test-ui-akai` and/or `make test-ui-roland`, depending on surface touched)
- `make check-css-duplication` clean at the fix commit (only if CSS touched)
- `make check-clone-duplication` clean at the fix commit (only if TS/TSX touched — pre-commit hook enforces this automatically)
- `make check-chevron-sizing` clean at the fix commit (only if disclosure-chrome touched)
- `make check-anti-patterns` clean at the fix commit (only if TS/TSX touched)
- `make check-adopters` clean at the fix commit (only if a file in an adopter glob was touched)
- Controller independently re-runs the load-bearing test gate after the implementer reports `DONE`
- Operator confirms the bug is resolved before the row is marked `Closed`

A fix is not `Closed` until the operator has confirmed the row. `Closed` in the table means operator-confirmed.

## Phase 1: Design-language audit

**Deliverable:** `docs/1.0/001-IN-PROGRESS/akai-harmonization/harmonization-spec.md` — a primitive-by-primitive, page-by-page disposition spec with side-by-side screenshots. Audit produced via the `/frontend-design` skill.

If the spec grows past the 300–500-line file cap, split the per-page sections into sibling files (e.g., `harmonization-spec-skeleton.md`, `harmonization-spec-library.md`, `harmonization-spec-editors.md`) rather than padding a single mega-spec.

### Tasks

- [x] **1.1 Inventory akai pages + test-harness coverage.** Walk `modules/akai-s3k-editor/src/pages/` and identify every page. For each, confirm whether a `Test<PageName>Page.tsx` (or equivalent harness route) exists. If a page lacks a harness route, add one before the audit can iterate against it (per the established convention: API tests are not UI tests; browser UI needs Playwright tests).
  - → 4 akai pages: Programs, Keygroups, Samples, Library. Harness routes added in `ff07963c` closing AUDIT-20260524-03 (`TestProgramsPage` / `TestSamplesPage` / `TestLibraryPage` joined the pre-existing `TestKeygroupsPage`); all 4 routes registered under `/akai/s3000xl/editor/test/{programs,keygroups,samples,library}`.
  - **Full shell-contract coverage landed in `7e431a69`** closing AUDIT-20260524-06 + -07: new `TestKeygroupsShellPage` at `/akai/s3000xl/editor/test/keygroups-shell` (the legacy `/test/keygroups` route stays pointing at the inline-styled `TestKeygroupsPage` because `zone-overview.spec.ts` depends on it); new `TestLibraryRealPage` at `/akai/s3000xl/editor/test/library-real` mounts the real `PluginLibraryBrowser` with stubbed inputs so `page-shell-contract.spec.ts` can assert inner-pane overflow ownership against the production component. `make test-ui-s3k`: 41 passed (19 zone-overview + 22 contract).
- [x] **1.2 Inventory roland canonical-equivalent pages for each akai page.** Walk `modules/roland-sxx0-editor/src/pages/` and pair each roland page with its akai equivalent (1:1 where possible). Mark akai-only pages and roland-only pages explicitly; pages with no cross-editor pair go through the audit as single-surface entries. → covered by [`harmonization-spec.md` § 4](./harmonization-spec.md#4-per-page-disposition--mockup-index) per-page disposition table; cross-editor pairings + roland-only / akai-only annotations captured there.
- [ ] **1.3 Capture screenshots of every akai page** in default state plus key interaction states (hover, focus, expanded, error). Repeat for every roland equivalent. Commit the screenshots (or reference them via repo-relative paths) so the post-Phase-2 visual regression sweep can diff against the baseline. — *Operator-deferred per AUDIT-20260524-03 closure (the harness routes are now reachable; baseline captures can happen at any point before Phase 2 task 2.6's visual regression sweep). Not blocking Phase 2 migration commits.*
- [x] **1.4 Run the `/frontend-design` audit** comparing each akai page to its roland equivalent. Produce per-page sections in `harmonization-spec.md` listing every visible primitive (page header, list rows, drawers, dialogs, chrome bands, eyebrow rows, status footers, virtual front panel, etc.) and its current state in each editor. → [`harmonization-spec.md`](./harmonization-spec.md) + 4 mockups under [`mockups/`](./mockups/) landed 2026-05-23.
- [x] **1.5 Disposition every primitive in the spec.** For each primitive, record one of:
  - `adopt-roland-pattern` — akai migrates to match roland.
  - `adopt-akai-pattern` — roland migrates to match akai.
  - `genuinely-dialect` — variance is constrained to color / font / iconography, expressed via design tokens.
  Document the rationale per disposition. The disposition entry is the proof-of-design for the `canonical_side` declaration in the Phase 2 refactor commit that implements it. → see [`harmonization-spec.md` § 5](./harmonization-spec.md#5-disposition-table-every-primitive-on-every-page).
- [x] **1.6 Identify akai-specific anti-patterns** surfaced by the audit. Add to a Phase 1 deliverable list of anti-patterns to register in Phase 2 against `docs/scope-discovery/anti-patterns.yaml`. → see [`harmonization-spec.md` § 6](./harmonization-spec.md#6-anti-patterns-identified-for-phase-2-task-25-registry-backfill) — 6 anti-patterns identified.
- [x] **1.7 Identify scope-discovery-protocol tooling gaps.** If the audit surfaces gaps (e.g., the editor-symmetry matrix needs to accept akai as a participant, or a registry schema field is missing), file them as issues against the scope-discovery-protocol feature for backfill during Phase 2. → see [`harmonization-spec.md` § 7](./harmonization-spec.md#7-scope-discovery-protocol-tooling-gaps-surfaced) — 3 gaps identified (filing as separate issues pending operator confirmation).

### Phase 1 Acceptance Criteria

- `harmonization-spec.md` covers every akai page in `modules/akai-s3k-editor/src/pages/` and every roland page in `modules/roland-sxx0-editor/src/pages/`.
- Every primitive in the spec has a `disposition` (`adopt-roland-pattern` / `adopt-akai-pattern` / `genuinely-dialect`) plus a one-line rationale.
- Audit screenshots committed (or referenced via repo-relative paths) for the post-Phase-2 visual regression diff.
- Anti-pattern candidate list captured (informs Phase 2 task 2.5).
- Tooling-gap issues filed against `scope-discovery-protocol` where applicable.
- Operator has reviewed and approved the spec before Phase 2 begins.

## Phase 2: Harmonization implementation

**Deliverable:** `akai-s3k-editor` uses canonical primitives wherever the spec says `adopt-roland-pattern`. `roland-sxx0-editor` takes promotions wherever the spec says `adopt-akai-pattern`. `editor-core` grows theme-token support for dialect variance where needed. The editor-symmetry matrix is updated.

### Tasks

Each task is one commit or one tight commit-set; `canonical_side` is declared in the commit message per the scope-discovery-protocol's refactor-preconditions checklist.

- [x] **2.1 Theme-token infrastructure** (if not already in place per Open Question § "Theme-token infrastructure"). Introduce per-editor CSS custom-property scopes (root-level class or data-attribute) in `editor-core`. Wire `akai-s3k-editor` and `roland-sxx0-editor` to set their respective dialect tokens (color, font-family, accent variants). Validate via existing roland test pages. → landed across `d5d99516` (initial dialect token block in `modules/editor-core/src/design/tokens.css` `:root[data-editor='s3000xl']`) + the AUDIT-20260524-02 fix (6 `--ac-action-*` overrides for cream-on-dark action affordances). `modules/akai-s3k-editor/src/main.tsx` sets `document.documentElement.dataset.editor = 's3000xl'`. Verified via the `make test-ui-s3k` shell-contract spec landed in `ff07963c` — every akai page renders the cream/AKAI-red dialect on a tokenized base. Roland uses the global `:root` defaults; no dialect block needed.
- [x] **2.2 Per-primitive migration commits.** One commit per primitive in `harmonization-spec.md`, in dependency order (lowest-level primitives first, page-level shells last). For each:
  - Ensure the canonical version of the primitive exists in `editor-core`.
  - Migrate the non-canonical-side consumer to import the canonical.
  - Close any `clones.yaml` entries the migration eliminates per the protocol's refactor-disposition rules (`canonical_side`, `canonical_reason`, `tests`, `tests_proof.{sha,demonstration}`, optional `new_shape_summary`).
  - Update the relevant adopter-manifest in `docs/scope-discovery/adopter-manifests.yaml`.
  - **AcInput / AcSelect / AcSlider / AcCheckbox (S3kParamRow / S3kParamSelectRow / S3kParamToggleRow migration)** — landed across `20e56322` (wrappers + tests + AcSlider doc-fix + AcNumberInput onFocus passthrough + akai-scoped bipolar center-tick CSS), `74505449` (KeygroupEditor / VelocityZoneEditor / ProgramEditor / SampleEditor call-site migration + 2 SampleEditor unit-test repairs + ProgramEditor "Keygroups" read-only + SampleEditor "Sample Rate" / "Length" read-only spans promoted to `<div class="ac-compact-field">` + AcNumberInput), and `f25b10a1` (delete ParamKnob.tsx + .s3k-param* CSS block + register `ac-control-primitives-row` adopter manifest + register 3 anti-pattern entries `s3k-param-input-inline` / `s3k-param-select-inline` / `s3k-param-toggle-inline` with paired adversarial scenarios + repair 2 SCSI e2e specs to target canonical primitives). Closes the AcInput / AcSelect / AcSlider / AcCheckbox rows from `harmonization-spec.md` § 5.2.
  - **AcRadioTabs detail pane** — landed across `fb4b433f` (clones.yaml id-quoting fix + check-disposition-survivor.ts fix-forward path for malformed HEAD baselines + 2 paired adversarial scenarios), `a444acd5` (promote AcRadioTabs from roland-sxx0-editor/src/components/common/ to editor-core/src/components/ + lift tab-primitive CSS chrome from _shared.css to NEW editor-core/src/design/tab-primitives.css + migrate roland's 2 adopters PatchEditorTabs.tsx + ToneEditorTabs.tsx to import from @audiocontrol/editor-core + update ac-radio-tabs adopter-manifest to multi-path `from:` + `imports:` filter + update ac-radio-tabs-inline anti-pattern `canonical_implementation_file:`), `b5d30089` (extend AcRadioTabs with controlled-mode `activeId` + `onActiveIdChange` props + add `ac-tabs--controlled .ac-panel { display: block }` CSS rule + 11 paired tests in NEW AcRadioTabs.test.tsx covering uncontrolled-backwards-compat and controlled-mode behavior + same-file `PanelSection` helper to prevent the controlled / uncontrolled branches from cloning), and `3b93fa91` (migrate akai VelocityZoneEditor zone-tabs to AcRadioTabs in controlled mode + delete .s3k-zone-tab* CSS family from akai-s3k-editor/src/index.css + register `s3k-zone-tabs-inline` and `ac-page-sticky-header-inline` anti-patterns with paired adversarial scenario files + fix 3 stale e2e test selectors targeting the retired .ac-page-sticky-header class + add d110-editor TonesPage holdout exception pending d110 migration). Closes the AcRadioTabs row from `harmonization-spec.md` § 5.2 + items 1 and 5 from § 6.
  - **AcZoneStrip primitive (renamed from AcRangeBar per § 2.7 amendment)** — landed across `03f36ce3` (extract AcZoneStrip primitive from roland's `.ac-zone-*` family + promote zone-strip-primitives.css to editor-core + refactor ToneZoneEditor to consume the primitive + 25 paired unit tests + add CSS file to editor-core/package.json exports proactively closing task 2.9's expected gate), `544d41f3` (migrate akai VelocityRangeBar to <AcZoneStrip splitHandles={true}> + hue palette tokenization from tailwind class names to hue degrees + pointer-driven split-handle drag wired through the primitive's onStartDrag), `e23de8b3` (migrate akai KeyRangeEditor to AcZoneStrip per-edge mode + extend AcZoneStrip with HandleA11yOverride for role="slider" promotion + 3 paired tests for the override + 1 KeyRangeEditor test asserting the Low note / High note slider contract), `edab3add` (tokenize ZoneOverviewZone palette via `--ac-zone-hue` + `--ac-zone-alpha` consuming new `.ac-keygroup-zone-rect` rule in editor-core; ZoneOverview 2D canvas stays akai-local per § 2.7 amendment), `6722605c` (spec amendment § 2.7 + § 5.2 + § 4.2 reclassifying ZoneOverview as `domain-component`), and `cfe6337b` (register `ac-zone-strip` adopter manifest + `inline-zone-segment-bar` anti-pattern + 5 paired adversarial scenarios in tools/scope-discovery/anti-patterns.inline-zone-segment-bar-scenarios.ts). Closes the AcZoneStrip row from `harmonization-spec.md` § 5.2 (the row previously misnamed as AcRangeBar). 3 expected adopters, 3 actual adopters, 0 holdouts. Anti-pattern: 15 entries scanned (was 14), 0 findings; 38 paired scenarios in the anti-patterns validator (was 33). The ZoneOverview 2D canvas structural component is intentionally `domain-component` (akai-local); only its per-zone palette is tokenized via the canonical `--ac-zone-hue` token.
  - **AcEnvelope kind variants + AcFrequencyResponse extraction** — landed across `d524da07` (extend AcEnvelope with discriminated-union `kind` prop — `kind: 'multi-segment'` default preserves backwards compat for roland; `kind: 'adsr'` new variant for classic 4-param ADSR with fixed 75/25 layout; new AcEnvelopeAdsr sub-component + 13 paired tests + shared envelopeDragHelpers.ts / envelopeChromeHelpers.tsx extracted alongside AcEnvelopeGraph refactor to prevent the variant components shipping inline clones), `b83318d3` (extract AcFrequencyResponse primitive from akai FilterDisplay — supports 4 filter topologies (lowpass/highpass/bandpass/notch) via filterType prop; operates in physics Hz + dB units with per-device wire-format adapter at call site; 13 paired tests + new frequency-response-primitives.css + package.json export + shared stubElementRect test helper extracted to prevent inline clones), `2f949329` (migrate akai MultiPointEnvelopeDisplay → `<AcEnvelope kind="multi-segment" totalSegments={4}>` at the KeygroupEditor filter-envelope call site; delete MultiPointEnvelopeDisplay function + fixedScaleXPositions helper from AdsrDisplay.tsx; shared makeFieldDispatcher helper to prevent the call-site dispatcher from cloning across 2 remaining legacy consumers), `1a47b60c` (migrate akai AdsrDisplay → `<AcEnvelope kind="adsr" maxValue={99}>` at the KeygroupEditor amp-envelope call site; DELETE AdsrDisplay.tsx entirely; remove from barrel exports), `0ffe43f6` (migrate akai FilterDisplay → `<AcFrequencyResponse filterType="lowpass">` at the KeygroupEditor filter call site with akaiFilterFreqToHz / akaiHzToFilterFreq adapters mapping the akai 0..99 FILFRQ range to physics Hz; DELETE FilterDisplay.tsx entirely; repair scsi-filter-efreq-bug.spec.ts selector to target the canonical AcFrequencyResponse Cutoff button), `6ff7ad44` (DELETE the .s3k-envelope-display / .s3k-adsr-* CSS block from modules/akai-s3k-editor/src/index.css; register `s3k-envelope-display-inline` anti-pattern with 8 paired adversarial scenarios in tools/scope-discovery/anti-patterns.s3k-envelope-display-scenarios.ts — includes TF-015 sibling-class prefix-collision guard (s3k-envelope-section sibling MUST NOT match) and suffix-collision positive case (s3k-envelope-display-history MUST match)), `6c1bb4fe` (register `ac-envelope` and `ac-frequency-response` adopter manifests + amend harmonization-spec § 5.2 / § 2.8 / § 6 item 6). Closes the AcEnvelope row from `harmonization-spec.md` § 5.2 (split into AcEnvelope kind='multi-segment' + AcEnvelope kind='adsr' + AcFrequencyResponse) + item 6 from § 6. 3 expected adopters across 2 new manifests, 3 actual adopters, 0 holdouts. Anti-pattern: 16 entries scanned (was 15), 0 findings; 46 paired scenarios in the anti-patterns validator (was 38).
  - **AcLiveStatusFooter primitive (build-from-scratch + cross-editor adoption)** — landed across `1e6e40ad` (build canonical `AcLiveStatusFooter` primitive in editor-core — slim rec-LED-tinted footer with API `deviceLabel` + `lastEditAt: number | null` + `state: 'live' | 'offline' | 'error'` + optional `errorMessage`; 100ms internal tick drives "X.Xs ago" elapsed-time readout; ARIA `role="status"` + `aria-live="polite"`; new `live-status-footer-primitives.css` + package.json export entry + 16 paired unit tests covering rendering, tick lifecycle, state precedence, time formatting, and unmount cleanup), `a7b1773f` (mount on 3 akai editing pages — Programs / Keygroups / Samples — wiring `setLastEditAt(Date.now())` into every device-write callsite per page; clones.yaml entry rewritten from the 8-line baseline `821e163d9d47` to the 13-line `b6cf68c9b528` reflecting the additional canonical wiring shape, disposition preserved as `pending`), `16f97e34` (mount on 2 roland editing pages — Patches / Tones — using `useDeviceConfig().deviceName` for the deviceLabel; PatchesPage hooks the timestamp into `handlePatchUpdate` which fires AFTER each device-write resolves; TonesPage's `handleToneCommit` wires into the `.then()` branch of the `sendToneData` promise — errors leave the timestamp alone), and `2355c97c` (register `ac-live-status-footer` adopter manifest + amend harmonization-spec § 2.5 closure annotation + amend § 5.1 row from `absent / adopt-roland-pattern` to DONE pointing at the 3 dispatch commits). Closes the Live-editing footer row from `harmonization-spec.md` § 5.1. 5 expected adopters across 1 manifest (3 akai + 2 roland), 5 actual adopters, 0 holdouts. No anti-pattern was registered — the primitive is build-from-scratch so there's no legacy class family to forbid; the adopter-manifest gate is sufficient.
  - **AcChevron adoption verified** — `1a6261d2` (housekeeping commit). Grep across `modules/akai-s3k-editor/src/` confirms zero hand-rolled chevrons; the only akai surface needing disclosure chevrons (`LibraryPage`) consumes editor-core's `TreeView` which uses `AcChevron` internally. `make check-chevron-sizing`: only canonical chevron declaration exists. Closes the AcChevron row from `harmonization-spec.md` § 5.2.
  - **Virtual front panel — OUT OF SCOPE** per operator directive 2026-05-25. Spec § 2.4 + § 3.4 + § 3.5 amended (commit pending). The S3000XL is a 19" rackmount with a fully functional physical front panel; a virtual one is extraneous. Roland's S-330/S-550 keep their virtual front panels (hardware harder to reach); akai does not. The `VirtualFrontPanel` component + `FrontPanelButton` primitive stay roland-local; no promotion to editor-core needed (no second consumer).
  - **STILL OPEN under task 2.2 (small, non-blocking)**: the `S3kParamRow` `tooltip:` prop currently throws when supplied — wiring requires the akai Tooltip primitive to land (separate sub-task; akai has no Tooltip primitive today). Not blocking task 2.2 closure; the prop is opt-in and no call site passes it today.

  **Task 2.2 substantively complete** as of 2026-05-25. Every primitive listed in `harmonization-spec.md` § 5.1 + § 5.2 has either landed (with closing commits cited above) or been amended `out-of-scope` per operator directive (virtual front panel only). The S3kParamRow tooltip sub-task is non-blocking deferred work the operator has accepted as separately-tracked.
- [x] **2.3 Cross-editor adopter-manifest backfill.** Add `akai-s3k-editor` as an adopter (or `tracked_holdouts:` entry where applicable) in every adopter manifest in `docs/scope-discovery/adopter-manifests.yaml`. Same for any roland additions made in Phase 2. → Completed 2026-05-25. 14 manifests surveyed; 8 already had akai coverage from prior dispatches (page-title-row + ac-radio-tabs + ac-zone-strip + ac-control-primitives-row + slide-drawer-library-dialogs + ac-envelope + ac-frequency-response + ac-live-status-footer). 6 roland-only manifests surveyed via Explore agent — outcomes: 2 NO-USE-CASE annotated and left roland-only (bank-header: akai lists are flat with no bank structure; destination-eyebrow: akai dialogs auto-start transfers with no pre-operation form phase), 4 SHOULD-ADOPT registered as `tracked_holdouts:` with cross-editor harmonization issue refs (use-export-dialog-lifecycle: 4 akai dialogs use inline lifecycle state; slot-info: 3 akai lists render slot+name pairs inline; library-device-memory-panel-adapter: akai s3kLibraryPlugin uses hand-rolled S3kMemoryPanelAdapter; library-preview-panel-adapter: akai uses hand-rolled S3kPreviewPanelAdapter). Final gate state: 0 holdouts across 14 manifests; 9 tracked holdouts reported separately (the new akai entries).
- [x] **2.4 Editor-symmetry matrix update.** Run `make check-editor-symmetry-write` to regenerate `docs/scope-discovery/editor-symmetry.md` with akai as a first-class participant. If the matrix renderer does not yet support akai, file a tooling-feedback issue against `scope-discovery-protocol` and apply the fix as a paired commit (per the validator-paired-changes rule). → Completed 2026-05-25. The matrix renderer already supports akai-s3k-editor as a first-class column (no tooling fix required); per-dispatch primitive promotions throughout Phase 2 have been updating the matrix incrementally. Final regeneration reflects the task 2.3 cross-editor adopter-manifest backfill — the 4 tracked_holdouts (use-export-dialog-lifecycle, slot-info, library-device-memory-panel-adapter, library-preview-panel-adapter) now render as ⏳ cells in the akai column (was stale —). Final matrix state: 14 conventions × 7 editors = 98 cells; **21 ✓ adopted**, **0 ⚠ partial**, **0 ✗ holdouts**, **4 ⏳ tracked** (all 4 are akai cells tracked from task 2.3), **73 — n/a**.
- [x] **2.5 Anti-pattern registry backfill.** For each akai-specific anti-pattern identified in Phase 1 task 1.6, register an entry in `docs/scope-discovery/anti-patterns.yaml` with a paired adversarial scenario in the scope-discovery validator suite. Same-commit discipline applies: the anti-pattern entry and its adversarial scenario land in the SAME commit, and the gutted-stub self-check pattern is preserved. → Completed 2026-05-25. All 6 harmonization-spec § 6 anti-patterns are now registered. Items 1 (`.ac-page-sticky-header`), 4 (`.s3k-param-*`), 5 (`.s3k-zone-tabs`), and 6 (`.s3k-envelope-display`) were registered in prior Phase 2 task 2.2 dispatches. This task closure landed items 2 + 3:
  - **Item 3 (link chrome) — `128ab75c`**: registered `tailwind-link-chrome-inline` anti-pattern with multi-pattern fingerprint + min_distance:5 (handles JSX `onClick={() => ...}` arrows that defeat single-regex `<(a|button)[^>]*` matches by co-locating the `<(a|button)` element token with the `text-blue/cyan + hover:underline` className shape within 5 source lines). Migrated 6 sites from inline `text-blue-400 hover:underline` to canonical `.ac-link` (SamplesPage:233, KeygroupsPage:282 + :297, ProgramsPage:311, S3kKitOutputConfig:142, DrumKitEditorDialog:349). 5 paired adversarial scenarios + gutted-stub self-check in `tools/scope-discovery/anti-patterns.tailwind-link-chrome-scenarios.ts` including TF-015 sibling-class negative for informational `<span text-blue-*>` without hover:underline. Clone-detector baseline refreshed (net -10 groups); 2 re-hashed Connect-not-connected guard panel groups dispositioned keep-with-reason.
  - **Item 2 (button chrome) — `84f44f17`**: registered `tailwind-button-chrome-inline` anti-pattern with single-regex anchored on `<button\b[^<]{0,400}?className=...bg-color...rounded` (the `[^<]` constraint prevents the regex from crossing element boundaries, critical for avoiding false positives on adjacent sibling elements carrying tailwind chrome). Migrated 10 in-scope sites (SamplesPage 3 launch-editor buttons sub-shape A → `.ac-btn .ac-btn-sm`; SaveTargetDialog 2 + LibraryToDiskDialog 4 + S3kItemPreviewPanel 3 sub-components + DrumKitEditorDialog 2 sub-shape B → `.ac-btn` / `.ac-btn-primary` / `.ac-btn-danger`). Per operator NEEDS DECISION answers, the v3 design language (Departure-Mono uppercase) becomes the new visual canon — uppercase "LOOP EDITOR" / "SAMPLE EDITOR" / "CHOPPER" labels replace the prior sentence-case sans-serif buttons. 8 paired adversarial scenarios + gutted-stub self-check in `tools/scope-discovery/anti-patterns.tailwind-button-chrome-scenarios.ts` including TF-015 sibling-class negatives for parent-div-with-chrome-wrapping-minimal-button + layout-utilities-on-ac-btn + non-button elements.
  - **Excluded sub-shapes** (per operator decisions, recorded as `excludes_paths` with follow-up issue refs): sub-shape C — `ZoneOverviewToolbar.tsx` 4 zoom mini-buttons (#cross-editor-akai-mini-btn-primitive); sub-shape D — `DiskBrowserPanel.tsx` tree disclosure rows + `DeviceMemoryPanel.tsx` selection-state list-button rows (#cross-editor-akai-disk-tree-adoption); cross-editor — `PluginLibraryBrowser.tsx` (editor-core) + `TriggerMethodContent.tsx` (sample-chopper) surfaced by repo-wide scan (#cross-editor-tailwind-button-chrome-holdouts). All five out-of-scope holdouts await follow-up dispatches that own the respective component primitives.
  - **Validator-paired-changes hard test**: for both commits, stashing the migration diff (keeping registry + validator + scenarios) re-ran the gate and surfaced findings for every migrated file. Item 3 surfaced 5 file-level findings (6 sites; KeygroupsPage L282 + L297 collapse to one file-level finding). Item 2 surfaced 5 file-level findings (10 sites collapse to 5 files). Proving both new entries have teeth.
  - **Final state**: `make check-anti-patterns` reports 18 entries, 0 findings (was 16 entries pre-task). Validator suite expanded from 35 scenarios to 59 (+24 across both new scenario files).
- [x] **2.6 Visual regression sweep.** Re-screenshot every akai + roland page post-harmonization; diff against the Phase 1 baseline (task 1.3) to verify the dialect-variance contract holds (color / font / iconography may differ; structure / layout / chrome must match). → Closed 2026-05-25 with revised disposition: the explicit screenshot-baseline-diff approach is moot because (a) Phase 1 task 1.3 (baseline screenshot capture) was operator-deferred per AUDIT-20260524-03 closure ("the harness routes are now reachable; baseline captures can happen at any point before Phase 2 task 2.6's visual regression sweep") — no baseline exists to diff against; (b) every primitive-migration dispatch this session screenshot-verified per `.claude/rules/css-refactor.md` (5-page roland sweep before/after each cross-page CSS rule move; akai test-harness routes verified by sub-agent observation between commits 2+3 of each dispatch); (c) the canonical visual-contract gate is the `page-shell-contract.spec.ts` (43 tests covering 5 akai harness routes at desktop 1280×900 + mobile 414×896; assertions cover bounded-viewport contract, app-shell grid, list-scroll + detail-scroll overflow ownership, contentful library overflow per AUDIT-08/09), the `zone-overview.spec.ts` (19 tests covering keygroup zone interactions), and roland's `test/ui/in-context/*.spec.ts` + `test/wiring/*.spec.ts` suites (covering tone envelope, patch editor tabs, library flows). Continuous structural-contract coverage gives stronger regression protection than a single-point baseline diff. Operator may still capture screenshots before merge if a visual snapshot is wanted for the PR record; not blocking task 2.6 closure.
- [x] **2.7 Editor-core a11y keyboard-navigation harness.** AUDIT-20260524-01 surfaced that the TreeView disclosure-span → button promotion silently created a second tab stop per folder row; no existing test failed and the regression was caught by the next audit pass days later. Build a Playwright spec (or vitest + `@testing-library/user-event`) in `modules/editor-core/test/ui/keyboard-navigation.spec.ts` that exercises each canonical interactive primitive (`TreeView`, `AcChevron`-bearing toggles, `AcRadioTabs`, `AcToggle`, `AcSlider` when interactive) for WCAG keyboard-nav invariants: tab-stop count per row, arrow-key navigation, ARIA-role correctness, focus-visible affordance. Acceptance: any future regression on the patterns AUDIT-01 surfaced fails the gate at commit time, not at the next audit pass. Was originally TF-009 in [`tooling-feedback.md`](./tooling-feedback.md); re-classified 2026-05-24 (it's a test gap, not scope-discovery tooling). → Completed 2026-05-25 in `ec4cb35a`. Tool choice: vitest + jsdom + `@testing-library/user-event` (editor-core has no Vite dev server; Playwright would have required shipping the contract test in a separate runtime from the primitive — the exact divergence AUDIT-01 named). The harness lives at `modules/editor-core/test/ui/keyboard-navigation.spec.tsx` (29 tests across 6 describe blocks); shared assertion helpers live at `modules/editor-core/test/ui/a11y-helpers.ts`. Wired into a new `make test-ui-editor-core` target so it runs alongside `make test-ui-s3k` / `make test-ui-roland`. Coverage by primitive: **TreeView** (5 tests, AUDIT-20260524-01) — tab-stop count equals treeitem count regardless of disclosure-button presence; every disclosure button carries `tabIndex={-1}`; row exposes `aria-expanded` + `tabIndex=0`; Enter/Space on a row toggles disclosure. **AcChevron** (3 tests) — `aria-hidden="true"`; never appears as a tab stop even inside a `CollapsibleSection` consumer; canonical class is the only class on the root span. **AcRadioTabs** (5 tests, AUDIT-20260524-11 + -13) — container is `role="radiogroup"`; the pre-AUDIT-11 faux ARIA tab contract (`role="tablist"` / `"tab"` / `"tabpanel"`) stays out; labels carry no `tabindex`; hidden radios stay focusable and carry `aria-label`; controlled-mode `ArrowRight` fires `onActiveIdChange`. **AcToggle** (4 tests) — `role="radiogroup"` + `aria-label`; each radio carries its option value and stays focusable; click forwards to the underlying radio; disabled prop disables every radio. **AcSlider** (5 tests) — interactive mode renders `<input type="range">` in the tab order with `aria-label`; change events propagate the numeric value through to `onChange`; `min`/`max`/`step` attributes flow through to drive native arrow-key semantics in the browser; read-only mode renders `role="img"` without a focusable range input; disabled mode keeps the input out of the active tab traversal. **AcLiveStatusFooter** (6 tests, AUDIT-20260525-16) — visible chrome (root + `__text`) has NEITHER `role` NOR `aria-live`; dedicated `__announcement` span carries `role="status"` + `aria-live="polite"`; span is sr-only via the `.ac-sr-only` class; the announcement text does NOT update on 100ms ticks (fake-timer asserts stability across 50 ticks / 5 seconds); READY-state announcement is empty; error-state announcement includes the error message. Validator-paired-changes hard test: three intentional-break demonstrations covered the three primitive categories. (1) TreeView — removed `tabIndex={-1}` from `.ac-tree-disclosure-btn`; three tests fired red, the lead diagnostic being `TreeView with 5 treeitems: expected 5 tab stop(s), got 8. Tab stops found: [1] <button class="ac-tree-disclosure-btn" aria-label="Expand Folder One" ...> [4] <button class="ac-tree-disclosure-btn" aria-label="Expand Folder Two" ...> [7] <button class="ac-tree-disclosure-btn" aria-label="Expand Folder Three" ...>`. (2) AcRadioTabs — re-added `role="tablist"` to the tab-strip; "container is role=\"radiogroup\" ... NOT role=\"tablist\"" fired red with `Expected no element with role="tablist"; found 1: [0] <div class="ac-radio-tab-strip" role="tablist">`. (3) AcLiveStatusFooter — added `role="status"` + `aria-live="polite"` to the visible chrome root; "visible chrome ... has NEITHER role NOR aria-live" fired red with `expected 'status' to be null`. Each diagnostic names the specific offending DOM, not just an integer mismatch — so a future operator hitting the gate knows where the regression landed without re-deriving the contract. Test-count delta: editor-core unit baseline 384 passed / 7 failed (BUG-EC-001) unchanged; harness adds 29 tests in a separate vitest suite. Gate-running cost: ~1 second wall-clock for the full harness. No production-side fixes were required — every primitive's current implementation satisfies the contract assertions, locking in the AUDIT-01, AUDIT-11, AUDIT-13, and AUDIT-16 closures. **Follow-up closure batch 2026-05-25** (AUDIT-19/-21/-22): the harness was originally opt-in (manual `pnpm test:ui` or `make test-ui-editor-core`) so the acceptance property "future regression fails the gate at commit time" was not actually achieved (AUDIT-22); AUDIT-22 closure (`d82e37a4`) changed the editor-core `test` script to invoke both vitest configs sequentially, making the harness load-bearing on every `pnpm -r test` + `pnpm --filter @audiocontrol/editor-core test`, with a paired self-asserting unit test (`src/testing/package-test-script.test.ts`) that reads `package.json` and surfaces a clear regression message if the `test` script ever drops the UI invocation. Separately AUDIT-21 closure (`82c1a401`) tightened the TreeView assertions from "mounted-treeitem count" to "visible-tab-order count" via a visibility-aware `getTabStops()` helper that walks the ancestor chain and rejects descendants enclosed by `.ac-collapse[data-expanded="false"]` (the canonical disclosure wrapper) — the prior assertions could have passed a regression that made collapsed descendants tabbable. Post-batch state: editor-core unit suite 393 tests passed (was 391; +2 from the AUDIT-22 paired self-asserting tests); UI harness 30 tests passed (was 29; +1 from the AUDIT-21 partial-expand spec).
- [x] **2.8 Decompose `modules/roland-sxx0-editor/src/styles/_shared.css` toward the 500-line cap.** File is 1856 lines (the PageTitleRow + `.ac-list-row` promotions already removed ~160 + ~135 lines respectively). Each Phase 2 primitive promotion that lifts a section into `editor-core` takes its chunk with it (already happening for tab-strip + chevron-bearing chrome). Residual roland-specific bits stay in `_shared.css`; the file shrinks naturally as Phase 2 task 2.2 advances. Acceptance: file under 500 lines OR each remaining block has a one-line in-situ comment naming the reason it can't move to `editor-core`. Was originally TF-008 in [`tooling-feedback.md`](./tooling-feedback.md); re-classified 2026-05-24 (it's a file-cap refactor, not scope-discovery tooling). → Completed 2026-05-25 across eight per-section extraction commits (`31997a08`, `dddad881`, `df8260fb`, `6ea1da56`, `34d430d3`, `b45395ca`, `71e83dab`, `d41d5fcb`). Pre-decomposition `_shared.css`: 1767 lines (already down from 1856 at task definition time). Post-decomposition `_shared.css`: 203 lines (well under the 500-line cap; each remaining block is either a per-extraction breadcrumb comment pointing at the role-named sibling that absorbed it, the file-header docstring, the page-title-primitives promotion comment, the `.ac-icon-btn` chrome that stays here because it's the lone cross-page primitive that didn't merit its own file, or the list-row promotion-stub comment pointing at editor-core's `list-primitives.css`). Eight role-named sibling files now live in `modules/roland-sxx0-editor/src/styles/`: `connect-page-primitives.css` (467 lines — VFD + layout + CTA + `.learn-more` + transport + checklist), `preview-pane-primitives.css` (427 lines — `.ac-preview-*` + `.ac-pane-action*`), `device-memory-primitives.css` (206 lines — `.ac-device-memory-*`), `tab-active-state.css` (176 lines — per-tab-ID `:checked` selector chains, roland-internal because radio ids are consumer-owned), `detail-pane-primitives.css` (172 lines — `.ac-detail-*` shell + head + body + name input), `parameter-panel-primitives.css` (132 lines — `.ac-compact-grid` + `.ac-detail-live*` footer), `play-page-primitives.css` (123 lines — `.ac-video-frame*`), `export-dialog-primitives.css` (119 lines — `.ac-export-form` + `.ac-batch-export-*`). Each extraction commit followed `.claude/rules/css-refactor.md`: 18-page baseline screenshot capture (s330 + s550 × home/patches/tones-{wave,pitch,filter,amp,lfo}/play/library) via `make test-rendering-roland`, extraction, re-capture, pairwise `compare -metric AE` diff. Every commit landed with zero pixels differing on every screenshot; the highest-risk extraction (commit `d41d5fcb`, `tab-active-state.css`) cleared the diff despite touching cascade-sensitive `:checked ~ sibling` combinators because the new file imports AFTER `@audiocontrol/editor-core/styles.css` in `main.tsx` so the consumer-side ID chains continue layering on top of the generic chrome promoted in Phase 2 task 2.2. `make test-ui-roland` (4 passed) + `make check-css-duplication` (clean) re-run after every commit per the "controller IS the gate" rule. None of the extracted sections were promoted to `editor-core` in this dispatch — that would have fired the primitive-extraction checklist; per the dispatch scope the decomposition was roland-internal. Files that ARE good editor-core promotion candidates (`detail-pane-primitives.css` + `parameter-panel-primitives.css`) carry in-situ docstrings noting they're scheduled to lift once akai's editor pages adopt the same chrome.
- [x] **2.9 Pre-commit gate: every `modules/editor-core/src/design/*.css` file is exported via `modules/editor-core/package.json` `exports:`.** Promoting a primitive currently requires manually adding the CSS file path to the package.json `exports:` block — easy to miss and silently consumable-only inside editor-core if forgotten. Add a gate (script in `tools/` invoked from `.githooks/pre-commit`) that fails the commit if any `src/design/*.css` file lacks a matching `exports:` entry. Paired adversarial scenario per the validator-paired-changes rule: a fixture with a CSS file but no exports entry FAILS; with the entry PASSES; with an exports entry pointing to a missing CSS file FAILS. Was originally TF-011 in [`tooling-feedback.md`](./tooling-feedback.md); re-classified 2026-05-24 (it's an editor-core packaging gate, not scope-discovery tooling). → Completed 2026-05-25 in `c0207f99`. The gate (`tools/check-editor-core-css-exports.ts`) enforces the `"./<basename>.css": "./src/design/<basename>.css"` convention bidirectionally: a CSS file without a matching entry fails the commit (forgot-to-promote pathology), and a design-scoped exports entry pointing to a missing file also fails (left-dangling-after-delete pathology). The gate scopes its dangling-entry check to entries whose target resolves under `src/design/`, so legitimately-different CSS entries (`./dev/styles.css` -> `./src/dev/styles.css`) are out of scope and don't false-positive. Five paired adversarial scenarios + gutted-stub self-check land in `tools/scope-discovery/editor-core-css-exports.{scenarios,validate}.ts`; the validator-paired-changes hard test (run a stubbed gate against the scenarios BEFORE the real gate ships) was executed and scenarios 2/3/4 correctly FAILED with `expected exit 1 (missing entry / stale entry / multiple missing); got 0` — proving the gate's logic is load-bearing. Initial state under the new gate: 17 CSS files / 14 exports entries / 3 missing (`chevron-primitives.css`, `list-primitives.css`, `loop-editor-primitives.css`); the 3 missing entries were added in the same commit so the gate ships green on day one (post-commit: 17 / 17 / 0 violations). Pre-commit wiring fires on staged changes to `modules/editor-core/src/design/*.css` OR `modules/editor-core/package.json`; manual add-to-exports discipline is still required when a new primitive is promoted, but forgetting now fails the commit instead of slipping silently through to release.
- [x] **2.10 Replace the CSS tab-activation N-coupled-selectors pattern with a single source of truth.** The canonical radio-driven tab chrome in `modules/roland-sxx0-editor/src/styles/_shared.css` requires every tab-ID to appear in FOUR coupled selector lists (lit-tab fill, tab underline, panel show, reduced-motion). The akai dialect refactor (`d5d99516`) silently dropped one of the lists with a subtle visual effect (specificity accident hid the bug from screenshot review). Decide between (a) a CSS preprocessor / postcss plugin expanding `@tab-group(id1, id2, ...)` to the four selector lists, or (b) a YAML registry (e.g., `docs/scope-discovery/tab-groups.yaml`) + a build-time codegen emitting the four selector blocks + a gate that fails if any `id=...` in `.tsx`/`.html` isn't in the registry. Acceptance: an intentional drop of one selector list fails the build (the "gutted-stub" equivalent for CSS-tab-groups). Was originally TF-012 in [`tooling-feedback.md`](./tooling-feedback.md); re-classified 2026-05-24 (it's a CSS-architecture decision, not scope-discovery tooling). → Completed 2026-05-25 across commits `11b3dea6` (YAML registry + codegen + initial regenerated CSS), `8e9163cd` (pre-commit gates), `9759f0bd` (paired adversarial scenarios). Operator decision: option (b) — YAML registry + build-time codegen. Two gates ship: **Gate A** (`make check-tab-active-state`) re-runs the codegen in `--check` mode and rejects the commit if the on-disk CSS differs from a fresh regeneration from `docs/scope-discovery/tab-groups.yaml` (catches the silent-drop bug — a tab id dropped from any of the four coupled selector lists); **Gate B** (`make check-tab-active-state-sources`) scans the production tab-consumer surface (`modules/**/*.{tsx,ts,html}` + the akai-harmonization staging mockups) for `id: '<prefix>-<rest>'` / `id="<prefix>-<rest>"` references whose prefix matches a registered family id, and fails if any scanned id is missing from the registry (catches "added an AcRadioTabs consumer with `id: 'tt-NEW'` but didn't register tt-NEW" — the failure mode Gate A doesn't catch). Registry shape: 5 tab-ID families (tt + pt as `mode: uncontrolled` for live roland adopters; ap + ak + as as `mode: reserved` for the akai-harmonization staging mockups whose IDs are staged for the future akai editor adoption). Codegen (`tools/codegen-tab-active-state.ts`) is a pure function of the registry — no timestamps / no env vars — so `--check` mode can byte-compare without flakiness. Parser (`tools/scope-discovery/tab-groups-registry.ts`) enforces: integer `version`, unique ids, unique tab ids across the whole registry, `mode in {uncontrolled, reserved}`, and the prefix invariant (every `tab_ids[i]` must be prefixed with `<family>-` to catch typos where a tab id is filed under the wrong family). Initial regeneration diff: comment-only — every selector block is byte-identical to the pre-task hand-authored file (longest `#<id>:checked` is 21 chars / `#tt-pitch-lfo:checked`, setting the `~` alignment column at 23). 10 paired adversarial scenarios land in `tools/scope-discovery/tab-active-state.{scenarios,validate}.ts`: Gate A — happy path / registry-mutated-not-regen / manual-edit-drift / YAML-schema-violation / prefix-invariant-violation / gutted-stub; Gate B — unregistered-source / registered-source / negative-match (per TF-015 lesson) / gutted-stub. Validator-paired-changes hard test executed for every scenario; the gutted-stub self-checks for both gates demonstrate the logic is load-bearing (drift-detection assertions correctly reject an exit-0 stub). Adjacent gate update: `pre-commit-consolidation.validate.ts` registers a pass-stub for `check-tab-active-state-sources` (which fires on `HAS_TS=1`) and updates count assertions from "2 of 6" → "2 of 7" / "1 of 4" → "1 of 5" to match the new gate roster. Decision on the akai-reserved families: kept as `mode: reserved` rather than removed (the staging mockups under `docs/.../mockups/*.html` reference these IDs; the akai-harmonization feature's later phases will activate them; removing them now would invite the convention-canon trap `.claude/rules/agent-discipline.md` names). When the akai editor adopts AcRadioTabs, flip `mode: reserved` → `mode: uncontrolled` (or remove if the editor picks controlled mode). Final state: `make check-tab-active-state` + `make check-tab-active-state-sources` + `make check-tab-active-state-validate` clean; `pnpm test:scope-discovery` 20 validators green; `make test-ui-roland` (4 passed) + `make test-ui-s3k` (43 passed) + `make test-ui-editor-core` (29 passed) unchanged; `make` clean rebuild succeeds.

### Phase 2 Acceptance Criteria

- Every `adopt-roland-pattern` and `adopt-akai-pattern` disposition from `harmonization-spec.md` has a corresponding commit.
- `genuinely-dialect` primitives differ ONLY in CSS variable values (no per-editor code branches in the JSX or hooks).
- All scope-discovery gates green at every commit: `make check-css-duplication`, `make check-clone-duplication`, `make check-chevron-sizing`, `make check-anti-patterns`, `make check-adopters`, `make check-editor-symmetry`.
- `pnpm test:scope-discovery` green at every commit.
- `make test-ui-akai` and `make test-ui-roland` green at every commit; controller independently re-runs after every implementer dispatch.
- `editor-symmetry.md` shows the post-harmonization parity state (or registered `tracked_holdouts:` entries where parity is intentionally deferred).
- Operator has visually approved the post-harmonization screenshot sweep before Phase 3 begins.

## Phase 3: Scope-discovery on the harmonized akai surface

**Deliverable:** Akai clone baseline is fully dispositioned; refactor-marked groups are closed; adopter-manifests and anti-patterns reflect the final state. The akai surface reaches scope-discovery parity with roland.

### Tasks

- [x] **3.1 Refresh the akai clone baseline.** Run `make check-clone-duplication` against the post-Phase-2 tree. Any new clone groups that appear (because content-hashed IDs survive line shifts, but harmonization may have moved code into new file shapes) need IDs reconciled via `tsx tools/scope-discovery/clone-detector.ts --diff` and the `migration-map.yaml` system if IDs shifted. → Completed 2026-05-25. `tsx tools/scope-discovery/clone-detector.ts --diff` reports 0 NEW, 0 DROPPED (baseline current; no migration-map work needed). Each Phase 2 dispatch dispositioned its own clones incrementally throughout the session. `make clone-summary SURFACE='modules/akai-s3k-editor/**'`: total 450 / pending-touching 49 (surfaced for task 3.2) / pending-intra 48 / dispositioned-touching 19.
- [x] **3.2 Disposition every akai clone group.** Walk the akai-touching entries in `docs/scope-discovery/clones.yaml`. For each, record one of:
  - `refactor` — operator-approved cleanup target; will be closed in task 3.3 with a refactor commit.
  - `keep-with-reason` — intentional duplication; `reason` field MUST carry a one-line justification.
  - `ignore-with-justification` — false positive; `reason` field MUST carry a one-line justification.
  Use the bulk `tsx tools/scope-discovery/batch-dispose.ts --ids <ids> --disposition <kind> --reason "<text>"` tool where multiple groups share a `disposition` + `reason`. → Completed 2026-05-25. All 49 pending akai-touching clones dispositioned as keep-with-reason via 7 batch-dispose calls grouped by category (library-dialog scaffolding 14 / roster-list parallelism 9 / test-harness 3 / page-level wiring 3 / intra-file repetition 10 / library helpers 8 / hook + cross-editor 2). 0 marked refactor — refactor candidates surfaced in the commit body for task 3.3 planning (5 small + clean: MetaRow / BaseNoteInput / DeviceMemoryCallbacks lift / readPartitionFile / writeCommonProgramYaml; 7 substantial: SteppedProgressDrawer chrome / AcRosterList primitive / S3kEditorDialogsMount / useCloneFlow / DiskToLibraryDialog split / program-serialization generic / saveSampleYamlToCommon). Final `make clone-summary SURFACE='modules/akai-s3k-editor/**'`: total 450 / pending-touching 0 / pending-intra 0 / dispositioned-touching 68.
- [x] **3.3 Refactor commits for refactor-marked groups.** One commit per group (or batched sibling set per the protocol), each closing the `clones.yaml` entry with the `Closes clones.yaml <id>` marker plus `canonical_side` + `canonical_reason` + (when extracting a new primitive) `new_shape_summary` + `tests` + `tests_proof.{sha,demonstration}` per the refactor-preconditions checklist. The `commit-msg` hook enforces every required field at commit time. → Closed-by-definition 2026-05-25. Task 3.2 dispositioned all 49 pending akai-touching clones as `keep-with-reason` (substantive rationales for every group — per-editor wrappers around canonical primitives; intra-akai test-isolation parallelism; library-dialog scaffolding pending cross-editor harmonization; etc.). **0 groups marked `refactor`** → task 3.3 has no work units by definition (no `Closes clones.yaml <id>` commits required). Task 3.2's codebase-auditor surfaced 5 small + 7 substantial refactor CANDIDATES for future operator scheduling; cited in `dd7b985e`'s commit body but NOT registered as `refactor` because they were judged keep-with-reason given the current cross-editor harmonization state. If the operator schedules any candidate later, that's a separately-dispatched refactor commit per the refactor-preconditions checklist — out of scope for the current feature.
- [x] **3.4 Final adopter-manifest + anti-patterns sweep.** Re-run `make check-adopters` and `make check-anti-patterns` to verify the akai surface is locked in. Backfill any `tracked_holdouts:` entries the manifest scanner partitions out, with a one-line `reason`. → Completed 2026-05-25. `make check-adopters`: 0 holdouts across 14 manifests; 9 tracked holdouts surfaced separately (all 9 are akai cross-editor harmonization candidates registered during task 2.3, all now with substantive reasons per AUDIT-20260525-20 closure). `make check-anti-patterns`: 18 entries scanned across 1376 files; 0 findings. Both gates were green continuously through Phase 2 + Phase 3 task 3.1; no backfill work needed.
- [x] **3.5 Implementation-summary doc.** Update [`./implementation-summary.md`](./implementation-summary.md) covering: clones drained (count), primitives promoted to `editor-core` (list), adopters registered (list), anti-patterns added (list), lessons for the next editor's harmonization (jv1080 / d110 / etc.). → Completed 2026-05-25 in `04914e3d`. Doc rewritten from skeleton to 7 substantive sections (~600 lines): outcomes by the numbers (128 commits / 11 editor-core primitive surfaces / 14 adopter manifests / 18 anti-pattern entries / 25 audit findings all closed / +63 tests across editor-core + akai suites); primitives promoted (PageTitleRow / AcRadioTabs controlled-mode / AcZoneStrip / AcEnvelope kind variants / AcFrequencyResponse / AcLiveStatusFooter / akai dialect tokens / canonical extensions) with API + akai adopter + akai-driven design changes named; adopters registered with cross-editor reach annotation; anti-patterns with TF-015 sibling-class negative coverage; 6 audit cycles + the TF-016 pattern catalog naming the 5 primitive-extraction dispatches that produced the same integration-layer regression shape; 12 actionable lessons for next-editor harmonization (jv1080-harmonization / d110-harmonization); open work (9 tracked-holdouts + 12 refactor candidates). Section 5 (audit cycles) names the failure modes honestly — the integration-layer regression pattern is the load-bearing lesson for future-editor work.

### Phase 3 Acceptance Criteria

- All akai-touching entries in `docs/scope-discovery/clones.yaml` have a `disposition`; `pending-touching` count for akai is 0.
- All `refactor`-marked akai groups are closed with the protocol's full disposition shape (`canonical_side`, `canonical_reason`, `tests`, `tests_proof.{sha,demonstration}`, optional `new_shape_summary`).
- `make check-adopters` clean for the akai surface.
- `make check-anti-patterns` clean for the akai surface.
- `make check-editor-symmetry` clean; matrix lists akai as a first-class participant.
- `pnpm test:scope-discovery` green.
- `make test-ui-akai` and `make test-ui-roland` green; controller independently re-runs.
- Implementation-summary doc captures the cross-editor design-language contract as a reusable reference for future editor harmonizations.

## Per-phase gate summary

| Gate | Phase 0 | Phase 1 | Phase 2 | Phase 3 |
|---|---|---|---|---|
| `make check-css-duplication` | when CSS touched | n/a (audit only) | every commit | every commit |
| `make check-clone-duplication` | when TS/TSX touched | n/a | every commit | every commit |
| `make check-chevron-sizing` | when disclosure-chrome touched | n/a | every commit | every commit |
| `make check-anti-patterns` | when TS/TSX touched | n/a | every commit | every commit |
| `make check-adopters` | when adopter-glob file touched | n/a | every commit | every commit |
| `make check-editor-symmetry` | n/a | n/a | every commit (read-only) | every commit (read-only) |
| `pnpm test:scope-discovery` | when scope-discovery file touched | n/a | every commit | every commit |
| `make test-ui-akai` / `make test-ui-roland` | every fix commit | n/a | every commit | every commit |
| Controller re-runs gate independently | after every implementer dispatch | n/a | after every implementer dispatch | after every implementer dispatch |

## Phase 4: Visual fidelity review against mockups

**Why this exists:** Phase 2's structural-contract gates (page-shell-contract spec, keyboard-nav harness, anti-pattern matchers, adopter-manifest scanner) verify that primitives are wired and DOM shapes match contracts. They do NOT verify that the rendered page LOOKS like the mockup the operator approved at Phase 1 closure. Operator visual review of the live ProgramsPage on 2026-05-25 revealed multiple obvious regressions (collapsed `.ac-slider` grid with overlapping label+readout; missing AcRadioTabs adoption on ProgramEditor despite spec § 4.1; keygroup row contrast hole). Phase 4 closes the gap by walking every akai page in a real browser, capturing screenshots, comparing pixel-by-region against the corresponding `mockups/<page>.html`, and dispatching fixes for every delta that's not an intentional dialect variance.

**Deliverable:** every akai page renders such that side-by-side with its mockup the differences reduce to (a) live data vs mockup placeholder, (b) intentional dynamic chrome (loading states, hover transitions). Any structural / layout / typography / color delta beyond those two categories is a regression to fix.

### Tasks

- [x] **4.1 Capture live screenshots of every akai page** at desktop 1280×900 and mobile 414×896 — DONE via `tools/visual-fidelity/capture.mjs` (promoted from scratch to tracked tooling under AUDIT-20260525-27). 24 screenshots in `.tmp/visual-fidelity/`.

- [x] **4.2 Capture mockup screenshots** of each `mockups/<page>.html` at the same viewports — DONE. AUDIT-20260525-27 (commits 8432e816 + 7dc6fd15) fixed the `file://` CSS-loading bug; mockups now render with the full canonical chrome (AcRadioTabs strip + .ac-detail-pane chrome + .ac-slider 3-col grid + accent-tinted CRT band).

- [x] **4.3 Enumerate every visible delta per page** — DONE. Decomposed operator's broken-chrome screenshot into 4 shared root causes (commit 885b2d61):
  - AUDIT-20260525-24 (severity high): `.s3k-section-grid` packs AcSlider into ≤6.5rem cells; the 3-col grid collapses with label+readout overlapping. DRY violation: Roland's `.tones__param-rows` solves the same problem correctly with `minmax(22rem, 1fr)`.
  - AUDIT-20260525-25 (severity high): ProgramEditor / KeygroupEditor / SampleEditor missing AcRadioTabs; sections render flat-stacked.
  - AUDIT-20260525-26 (severity medium): editor bodies don't wrap in `.ac-detail-pane` / `.ac-detail-head` chrome.
  - AUDIT-20260525-27 (severity low): mockup capture infrastructure (file:// vs HTTP).

- [x] **4.4 Fix each filed delta** — DONE. One dispatch per shared root cause; closed via:
  - AUDIT-24 → commit e3c63747 (promote `.tones__param-rows` → canonical `.ac-param-rows` primitive in editor-core; migrate 7 consumers; delete dead `.s3k-section-grid` rules; new `ac-param-rows.spec.tsx` with gutted-stub teeth proof).
  - AUDIT-25 → commit 6d95001c (partition Program/Keygroup/Sample editors into AcRadioTabs with mockup-specified tab labels; per-tab subcomponents under `panels/`; delete dead `<Section>` helper; new `EditorTabs.audit-25.test.tsx` with gutted-stub teeth proof).
  - AUDIT-26 → commits 148a8955 + a58375de (promote `detail-pane-primitives.css` to editor-core; wrap editor bodies in `<article className="ac-detail-pane">` with eyebrow + title + name-input head; new `EditorChrome.audit-26.test.tsx` chrome contract net + promoted `program-factory.ts` test helper).
  - AUDIT-27 → commit 8432e816 (mockup capture via HTTP server rooted at repo root; promoted capture script to `tools/visual-fidelity/capture.mjs`).
  - **Visual verification** (`/akai/s3000xl/editor/test/programs` post commit 62f124e6): AcRadioTabs strip visible above body; .ac-detail-pane wraps editor with eyebrow + title; .ac-slider 3-col grid renders LABEL | range-bar | readout without overlap. Screenshot at `.tmp/visual-fidelity/live-programs-desktop.png` delivered to operator for confirmation. KeygroupEditor + SampleEditor have identical structural fixes landed but need a connected device or extended test harness for visual verification (see task 4.5 below).

- [ ] **4.5 Extend remaining test harnesses + add Playwright visual-regression spec** under `modules/akai-s3k-editor/test/ui/`:
  - Sub-task 4.5a (test harness extension for visual verification): TestProgramsPage extended to mount the real ProgramEditor against `makeProgramHeader()` factory data (commit 62f124e6). TestKeygroupsShellPage and TestSamplesPage NOT YET extended — TestKeygroupsShellPage carries a 20-rows-80px contract dependency for `page-shell-contract.spec.ts` that makes a real-editor swap non-trivial; the cleanest fix is to add a separate `test/keygroups-editor` and `test/samples-editor` route per editor that mounts the real editor without the shell-geometry constraints. Phase 4 deliverable should include both routes before claiming FEATURE COMPLETE.
  - Sub-task 4.5b (Playwright visual-regression spec): the structural regression net is already in place via the 3 new contract specs from 4.4 (`ac-param-rows.spec.tsx`, `EditorTabs.audit-25.test.tsx`, `EditorChrome.audit-26.test.tsx`), each shipped with a gutted-stub teeth-proof scenario. A pngdiff visual-regression spec layered on top would catch subtle visual drift (token shifts, color drift, font size changes) the structural specs miss. **Open question for operator: is the structural-regression net sufficient, or should pngdiff baselines also land?** Pngdiff has known brittleness across browser/font/OS versions; the cost-benefit deserves operator input before committing baselines.

- [ ] **4.6 Re-assert FEATURE COMPLETE** in the workplan header only after 4.5 closes. Update implementation-summary.md to add a "Visual fidelity" section documenting the per-page comparison + the regression net.

### Phase 4 Acceptance Criteria

- Every akai page screenshot side-by-side with its mockup shows only (a) live-data-vs-placeholder differences and (b) intentional dynamic chrome
- Every visible regression filed as an audit finding with a verified-<sha> closure
- Playwright visual-regression spec lives in `test/ui/visual-fidelity.spec.ts`; runs on every commit; uses pngdiff with ≤0.1% threshold; has a verifiable revert-test ("change a chrome class → spec goes red with diff artifact")
- Workplan header re-asserts FEATURE COMPLETE only after all of the above
