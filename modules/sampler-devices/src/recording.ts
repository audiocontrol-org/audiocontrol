/**
 * Public entry for `@audiocontrol/sampler-devices/recording`.
 *
 * Re-exports the recording-layer surface (fixture schema today; recording
 * proxy adapter and write helpers will land in subsequent Phase 0 tasks).
 */

export * from './recording/index.js';
