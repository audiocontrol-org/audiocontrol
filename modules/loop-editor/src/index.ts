/**
 * Loop editor — algorithm and type exports (no React dependency).
 *
 * This entry point can be imported without React:
 *   import { ... } from '@audiocontrol/loop-editor'
 */

export type {
  LoopCandidate,
  SearchConfig,
  LoopDetectionProgress,
} from './types';
