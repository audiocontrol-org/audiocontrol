feat(akai-s3k-editor): Phase 2 task 2.2 — adopt .ac-page-shell--fixed-viewport + .ac-app-shell + .ac-list-row chrome

All 4 akai pages migrated from the hand-rolled .ac-page-shell (no
modifier) + .ac-list-detail-grid + .ac-list-column-sticky shape to
the canonical .ac-page-shell--fixed-viewport + .ac-app-shell pair.
3 akai list components (ProgramList / KeygroupList / SampleList)
restructured to use the canonical .ac-list + .ac-list-scroll +
.ac-list-row chrome.

Pages updated (main render path only; not-connected / loading
fallbacks stay on .ac-page-shell without modifier per design):
  - ProgramsPage.tsx
  - KeygroupsPage.tsx
  - SamplesPage.tsx
  - LibraryPage.tsx (hand-rolled `style={{ height: 'calc(100vh - 8rem)' }}`
    replaced with .ac-page-shell-body wrapping PluginLibraryBrowser —
    matches roland LibraryPage canonical shape)

List components rewritten:
  - ProgramList.tsx — .ac-list + .ac-list-scroll outer chrome;
    .ac-list-row rows with .ac-list-slot / .ac-list-name cells;
    aria-selected / role=button / tabIndex=0 wiring preserved
  - KeygroupList.tsx — same; slot-width overridden to 2.75rem for
    "KG10"/"KG11" 4-char labels (canonical default 2.25rem fits 3-char
    slots but not 4-char)
  - SampleList.tsx — same; default slot width fits "001"–"062"

Cross-module CSS promotion (sub-agent expanded scope beneficially):
  - .ac-list-row + its state rules (:hover, [aria-selected="true"],
    [aria-selected="true"] .ac-list-slot, :focus-visible,
    [aria-disabled="true"], action-reveal trio) and .ac-list-meta
    PROMOTED from roland-sxx0-editor/src/styles/_shared.css to
    canonical editor-core/src/design/list-primitives.css. Cross-editor
    consumers (akai now, jv1080 / d110 later) adopt without per-module
    forking. _shared.css gets a pointer comment to the new home.

Akai-local CSS additions (extensions on top of canonical, with
promotion criteria documented in each block's comment):
  - .ac-detail-scroll — right-pane scroll wrapper paired with
    .ac-app-shell's height contract. Akai-specific because the .s3k-*
    editor surfaces inside need it; promote to editor-core if a sibling
    editor lands without a .ac-detail-pane wrapper.
  - .ac-akai-list-eyebrow* — list title row above .ac-list-scroll
    (akai lists are flat — no banks — so a simple title eyebrow
    replaces the .ac-list-bank-header chrome roland uses).
  - .ac-akai-list-row + variants — extends canonical .ac-list-row
    with the per-row action cluster (refresh / clone / delete on
    hover) + the inline rename-input chrome. Roland retired per-row
    actions in the 2026-05-19 sweep so these don't need to be
    canonical yet.

Removed:
  - modules/akai-s3k-editor/src/index.css — the .ac-list-detail-grid
    18rem override at the 1024px breakpoint (akai pages no longer use
    that primitive; .ac-app-shell column width is 16rem via
    --ac-app-shell-list-col).

Tests updated:
  - SampleList.test.tsx — two assertions migrated from
    .toContain('bg-blue-600') (the old Tailwind selected-row class) to
    [aria-selected="true"] / "false" (the canonical contract owned by
    the new .ac-list-row[aria-selected] rule).

Baseline:
  - clones.yaml refreshed via `make refresh-clones-baseline` after the
    file rewrites shifted line ranges (12 dropped from old line-shifted
    ranges, 7 new from refactored ranges; the new entries are dispositioned
    pending and will be walked in Phase 3 task 3.2).

Mobile escape hatch from AUDIT-20260524-03 means below 900px the
.ac-page-shell--fixed-viewport falls back to auto height — verified
the akai pages don't break on mobile.

Per harmonization-spec § 5.1 / § 5.3 disposition "adopt-roland-pattern"
for .ac-page-shell--fixed-viewport, .ac-app-shell, .ac-list,
.ac-list-row, .ac-list-bank-header (akai uses .ac-akai-list-eyebrow
instead — banks aren't a concept in akai).

Sub-agent: ui-engineer dispatch executed the migration; controller
ran gates independently before commit.

Verification (controller-side):
  make: clean
  make test-ui-roland: 4 passed + 2 skipped (matches baseline)
  make test-ui-s3k: 19 passed (zone-overview specs all green)
  make check-anti-patterns: 0 findings
  make check-adopters: 0 holdouts (9 tracked pre-existing)
  make check-clone-duplication: 0 NEW, 0 DROPPED (post-refresh)
  make check-chevron-sizing: clean

Pre-existing editor-core test failures (7) + akai loading-status test
failure (1) unchanged — verified unrelated to this commit.

Part of #460 (Phase 2 harmonization implementation).
